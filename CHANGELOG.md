- 7/10 : Got up and running on xy.michaeltowle.io. CF Access OTP implemented. Temporary /sing page enabled to confirm Anthropic API correctly wired.
- Next: MVP scope at /mvp route. Provide one problem screenshot, Haiku returns variations on problem. Solve or skip, no selecting. Save generated-problem, problem-attempt to DB. Give yourself a trophy. Track wrongs. Track time. If skipped, don't save. No sympy, no bucketing, no telling it how to tweak the original. Offer 'generate more' at end of batch. Problems can be redone via trophy selection, but batches can't be replayed. Keep batch_id field alive in generated-problem so that if, for whatever reason, a batch is deemed untrustworthy, the associated generated-problems can be quarantined.
